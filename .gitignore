# Node.js modules (if you are using Node.js for Tailwind setup)
node_modules/

# Tailwind config files that are auto-generated
tailwind.config.js

# The build output directory (often used in projects with build steps)
dist/
build/

# CSS files that are generated during the build process (if you're using a CSS preprocessor like PostCSS)
*.css.map

# Allow manually created CSS files
!*.css

# Environment variables (for sensitive information like API keys)
.env

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific files
.DS_Store

# VSCode settings folder (optional)
.vscode/

# Cache or generated files
.cache/

# Lock files (optional but usually safe to ignore)
package-lock.json
yarn.lock
